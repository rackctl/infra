name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: opentofu/setup-opentofu@v1
        with:
          tofu_version: "1.11.0"
      - name: fmt
        run: tofu fmt -check -recursive iac
      - name: validate deploy component
        run: |
          tofu -chdir=iac/components/aws/deploy init -backend=false -input=false
          tofu -chdir=iac/components/aws/deploy validate
